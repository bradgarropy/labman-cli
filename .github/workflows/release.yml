name: "🚀 release"

on:
    release:
        types: [published]

jobs:
    release:
        name: "🚀 release"
        runs-on: ubuntu-latest
        steps:
            - name: "🛠 build"
              uses: ./.github/actions/build
            - name: "🚀 publish"
              run: npm publish
              env:
                  NODE_AUTH_TOKEN: ${{secrets.NPM_AUTH_TOKEN}}
